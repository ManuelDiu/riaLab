import { TipoDocumentoService } from './tipo-documento-service';

describe('TipoDocumentoService', () => {
  it('should create an instance', () => {
    expect(new TipoDocumentoService()).toBeTruthy();
  });
});
