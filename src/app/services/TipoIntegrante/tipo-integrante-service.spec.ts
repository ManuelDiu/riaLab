import { TipoIntegranteService } from './tipo-integrante-service';

describe('TipoIntegranteService', () => {
  it('should create an instance', () => {
    expect(new TipoIntegranteService()).toBeTruthy();
  });
});
